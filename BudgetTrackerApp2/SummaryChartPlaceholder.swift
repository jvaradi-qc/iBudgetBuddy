import SwiftUI

struct SummaryChartPlaceholder: View {
    var body: some View {
        VStack {
            Spacer()
            Text("Summary Chart Coming Soon")
                .foregroundColor(.secondary)
            Spacer()
        }
    }
}
